import React from "react";
import { Product } from "../../components";

const type = "phòng";

const RoomRental = () => {
  return (
    <Product type={type}/>
  );
};
export default RoomRental;
